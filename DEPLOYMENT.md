# TagSeeker 배포 방법

## 가장 간단한 방법

프로젝트 폴더에서 PowerShell을 열고 다음 명령을 실행합니다.

```powershell
.\build-release.ps1
```

- 실행에 필요한 .NET을 포함한 Windows x64 배포 파일은 `bin\publish\TagSeeker-win-x64`에 생성됩니다.
- Inno Setup 6이 설치되어 있으면 `installer\output`에 설치 프로그램도 함께 생성됩니다.
- 설치 프로그램이 필요 없으면 `.\build-release.ps1 -SkipInstaller`를 사용합니다.

## 설치 프로그램의 동작

- 현재 사용자 영역에 설치하므로 관리자 권한이 필요하지 않습니다.
- 바탕 화면 바로 가기는 설치 중 선택할 수 있습니다.
- 이미지 우클릭 메뉴 추가도 설치 중 선택할 수 있습니다.
- 사용자의 기본 이미지 앱은 자동으로 바꾸지 않습니다.
- 제거할 때 앱의 설정, 태그, 번역 기록과 캐시는 보존합니다. 앱 설정의 각 데이터 관리 기능으로 직접 삭제할 수 있습니다.

## 경로를 전달해서 실행

실행 파일에 폴더 경로를 전달하면 그 폴더를 열고, 지원하는 이미지 경로를 전달하면 이미지 뷰어로 바로 엽니다.

```powershell
.\TagSeeker.exe "D:\Images"
.\TagSeeker.exe "D:\Images\sample.webp"
```

## 배포 전 권장 확인

1. 이미지와 폴더 경로에 한글 및 공백이 포함된 경우를 확인합니다.
2. PNG, JPEG, WebP, GIF, TIFF, TGA 등 주요 형식을 각각 확인합니다.
3. OCR 최초 실행과 번역 방식을 확인합니다.
4. 설정, 태그, 캐시 및 오류 기록 기능을 확인합니다.
5. 설치 후 실행과 제거를 별도의 Windows 사용자 환경에서 확인합니다.
