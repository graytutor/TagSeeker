<Window x:Class="CustomImageViewer.TextInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="440" Height="175" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="#181B21" Foreground="#F4F4F5">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="PromptText" Margin="0,0,0,8"/>
        <TextBox x:Name="ValueBox" Grid.Row="1" Padding="8,6"
                 Background="#1C2027" Foreground="#F4F4F5" CaretBrush="White"
                 BorderBrush="#3A404A" KeyDown="ValueBox_KeyDown"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right"
                    VerticalAlignment="Bottom">
            <Button Content="취소" Width="82" Margin="4" IsCancel="True"/>
            <Button Content="확인" Width="82" Margin="4" IsDefault="True" Click="Accept_Click"/>
        </StackPanel>
    </Grid>
</Window>
